<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<li><%= link_to("Back to main", tasks_path(@tasks))%>
<li><%= link_to("Edit task", edit_path(@task))%>
<li>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
</li>
